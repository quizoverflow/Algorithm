#include <bits/stdc++.h>
#define TIMME ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
using namespace std;
int main(){
    TIMME
    int i, ans, k;
    int bound[6][2];
    cin >>k;
    for(i = 0; i < 6; ++i) cin >> bound[i][0] >> bound[i][1];
    

    
    
    return 0;
}